> `python` // activar el modo ejecución modo python deben quedar estos simbolos `>>>>` <br>
> `python -m pip install nltk` // instalar la libreria nltk en la máquina desde powershell
> `import nltk` //importar libreria para lenguaje natural <br>
> `nltk.download('punkt')` // descargar la libreria `nltk` <br>
> `ctrl+z enter` //salirse del editor de python <br>
> `!pip install scikit.learn nltk numpy gym==0.25.2 gym-notices` //instalar librerias colab <br>
> `pip install scikit.learn nltk numpy gym==0.26.2 gym-notices` //instalar librerias visual <br>
>