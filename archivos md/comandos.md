`COMANDOS DEL CURSO`

> `descargar e instalar el Git` //decarga  einstalar el paquete Git
> `git --version` //version del git instalada desde terminal <br>
> `code .` // ejecutar visual code desde CMD <br>
> `git init` // iniciar un repositorio vacio en git <br>
> `git status` //para verificar el status de git<br>
> `git add .` //adicionar archivos al repositorio git <br>
> `Crear la cuenta en GitHub` //crear cuenta en GitHub y crear el nuevo repositorio
> `git config --global user.email "click.bienesraices7@gmail.com"` // para enlazar el git con el correo del github, si el quipo donde se esta trabajando nunca se ha regitrado en GitHub  <br>
> `git config --global user.name "martincastano"` //dar el nombre del usuario al git, si el quipo donde se esta trabajando nunca se ha regitrado en GitHub <br> 
> `git commit -m "first commit"` // check point del repositorio, tome los cambios en la configuracion <br>
> `git branch -M main` //definir la rama del proyecto github ( si solo se ejecuta git branch, toma la rama master) <br>
> `git remote add origin https://github.com/clickbienesraices818/BCMartinCastano.git` // ligar el proyecto en GitHUb sto se oma delinmción gnrda po el gihub cuando se crea el repositorio<br>
> `git push -u origin main (master)` // enviar al repositorio y confirmar la propiedad <br>
> `git clone URL` //clonar un repositorio de Git Hub de la URLcd .. <br>
> `descargar e instalar el python` <br>
> `git config user.email` //verificar el correo sobre el que esta un git <br>
> `git pull origin main` //(Replace main with your branch name if it is different, like master or a feature branch). <br>


`//---------------- COMANDOS PYTHON ----------------------------------------<br>`

> `python --version` //verificar la versión del python instalado <br>
> `python -m venv env312` // crear el entorno virtual de trabajo en python, env312 es el nombre del entorno <br>
> `env312\scripts\activate` //activar el entorno - se ejecuta el archvio `activate` ubicado en esa ruta <br>
> `Set-ExecuAQtionPolicy Unrestricted`  //para habilitar los cambios al SO con la ejecución de comandos. <br>
> `Set-ExecutionPolicy Unrestricted -Scope CurrentUser` /para habilitar los cambios al SO con la ejecución de comandos. <br>
> `pip list` para listar las librerias instaladas <br>
> `crear un archivo .gitignore dentro del repositorio` <br>
> `deactivate` //desacvtivar el entorno <br>
> `NOTA: Siempre hay que activar el ambiente de trabajo con el comando env312\scripts\activate` <br>

> ## librerias para python

> `pip install pandas numpy matplotlib` // `pandas` --> manejo de datos -- `numpy` --> cálculo matemático -- `matprolib` -- > graficos <br>
> `python.exe -m pip install --upgrade pip` // actualizar el pip <br>
> `git remote -v`  //ver en donde esta enlazado el envairoment <br>
> `pip list` // listar las librerias que estan instaladas <br>
> `pip freeze > requirments.txt` // enviar al archivo requirments.txt las librerias instaladas con su versión <br>
> `pip install -r requirements.txt` //replicar el enviroment que esta en el arvhico requirments.txt (NOTA: hay que crear el nuevo enviroment, para instalar las librerías en ese enviroment)
> `pip install flask` // framework de Python para desarrollo WEB
> `pip install scikit-learn`